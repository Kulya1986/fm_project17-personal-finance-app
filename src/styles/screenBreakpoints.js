export const SIZES = {
  xs: 400, // for small screen mobile
  sm: 724, // for mobile screen
  md: 900, // for tablets
  lg: 1280, // for laptops
  xl: 1440, // for desktop / monitors
};

export const DEVICE = {
  xs: `(max-width: ${SIZES.xs}px)`,
  sm: `(max-width: ${SIZES.sm}px)`,
  md: `(max-width: ${SIZES.md}px)`,
  lg: `(max-width: ${SIZES.lg}px)`,
  xl: `(max-width: ${SIZES.xl}px)`,
};
